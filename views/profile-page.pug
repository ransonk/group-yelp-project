extends layout

block content
  div.profile__nav-bar
      div(class="profile__nav-logo" onclick="window.location.href='/'")
      button(id='sign-up' class='nav__btn' onclick="window.location.href='/sign-up'") Sign Up
      button(id='log-in' class='nav__btn' onclick="window.location.href='/log-in'") Log In
      button(id='profile' class='nav__btn') Profile
      button(id='log-out' class='nav__btn' onclick="window.location.href='/log-in'") Log Out
      button(id='my-business' class='nav__btn') My Business
  div.profile__main
    div.user-reviews-container
      div.profile__user-container
        button.profile__picture-edit Edit Profile Picture
        form(id="profile-picture-edit-form" for='profileUrl')
          label(for="profile-picture-edit")
          input(id="profile-picture-edit" type="text" placeholder="paste your picture url here" name="profileUrl" class="hidden")
        button(type="submit" id="profile-picture-edit-button" class="hidden") Edit
        button(id="profile-picture-edit-cancel" class="hidden") Cancel
        div.profile__user-pic
        div.profile__user-name
        div.profile__user-location
      div.profile__reviews-container
  script(type="module" src="/js/users.js")
